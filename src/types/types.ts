export interface SliderValues{
    seaCost: number,
    riverCost: number,
    landCost: number, 
    changeCost: number,
    size: number,
}

export interface Point{
    row: number,
    col: number
}